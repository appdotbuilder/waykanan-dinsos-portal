import { type ApplicationDocument } from '../schema';

export async function getApplicationDocuments(applicationId: number): Promise<ApplicationDocument[]> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all documents associated with a specific application.
  // Used by both citizens (to see their uploaded docs) and staff (to review documents).
  return Promise.resolve([]);
}